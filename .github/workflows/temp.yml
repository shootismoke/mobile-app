name: Show Me the S3cr3tz
on: [pull_request]

jobs:
    debug:
        name: Debug
        runs-on: ubuntu-latest

        steps:
            - name: Check out code
              uses: actions/checkout@v2

            - name: Set up secret file
              env:
                  SIS_PRODUCTION_GOOGLE_SERVICES_JSON: ${{ secrets.SIS_PRODUCTION_GOOGLE_SERVICES_JSON }}
                  SIS_PRODUCTION_IOS_GOOGLE_MAPS_KEY: ${{ secrets.SIS_PRODUCTION_IOS_GOOGLE_MAPS_KEY }}
                  SIS_PRODUCTION_ANDROID_GOOGLE_MAPS_KEY: ${{ secrets.SIS_PRODUCTION_ANDROID_GOOGLE_MAPS_KEY }}
              run: |
                  echo $SIS_PRODUCTION_GOOGLE_SERVICES_JSON >> secrets.txt
                  echo $SIS_PRODUCTION_IOS_GOOGLE_MAPS_KEY >> secrets.txt
                  echo $SIS_PRODUCTION_ANDROID_GOOGLE_MAPS_KEY >> secrets.txt

            - name: Run tmate
              uses: mxschmitt/action-tmate@v2
